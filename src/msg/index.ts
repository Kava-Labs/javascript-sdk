import { cosmos } from './cosmos';
import { kava } from './kava';
import { hard } from './hard';
import { swap } from './swap';
import { evm } from './evm';

export const msg = {
  cosmos,
  kava,
  hard,
  swap,
  evm
};
